# Server Configuration
PORT=8000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=*

# Supabase Configuration
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Storage / assets
USER_ASSET_BUCKET_NAME=user-assets
REFERRAL_SHARE_BASE_URL=https://pocket-llm-api.vercel.app/download

# Encryption
# Accepts either a Fernet token or a passphrase (a Fernet key will be derived automatically).
ENCRYPTION_KEY=a_strong_32_byte_secret_key_for_encrypting_api_keys

BACKEND_BASE_URL=https://pocket-llm-api.vercel.app/
