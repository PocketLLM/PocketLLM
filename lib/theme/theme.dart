/// Barrel file for the PocketLLM theme layer.
///
/// Re-exports all theme definitions so that consumers can import a single file
/// (`package:pocketllm/theme/theme.dart`) instead of reaching into individual
/// modules.
export 'app_colors.dart';
